import type { referencesType } from "@/common/types/molecules/references.type";


const References = (props : referencesType ) => {
    return (
        <div>

        </div>
    )
}

export default References;